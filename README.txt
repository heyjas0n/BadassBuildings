Spring MVC app to configure and add buildings.

Also uses Hibernate and JPA annotations.

JDBC drivers are currently configured for MySQL.

The war file can be deployed to Tomcat EE, (not sure about Tomcat 6/7)